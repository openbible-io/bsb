name: Pull upstream
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
permissions:
  contents: write

jobs:
  pull-upstream:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
      - run: npm install
      - run: ./download.sh
      - name: Push and publish
        run: |
          git config user.name ci
          git config user.email ci@openbible.io
          if [[ -n "$(git status -s)" ]]; then
            node build.js
            VERSION=$(npm version patch --no-commit-hooks)
            git add .
            git commit -m 'Download + parse new version'
            git tag $VERSION
            git push --tags origin master
            npm publish --provenance --access public
          fi
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
