name: Pull audio
on:
  schedule:
    - cron: '0 0 * * *'
  workflow_dispatch:
permissions:
  contents: write

jobs:
  pull-audio:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: denoland/setup-deno@v2
      - uses: FedericoCarboni/setup-ffmpeg@v3
      - run: ./build/pull-audio.sh
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
